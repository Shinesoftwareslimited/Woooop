// Optional JavaScript if you want to track clicks or add dynamic functionality
document.querySelectorAll('.download-btn').forEach(button => {
    button.addEventListener('click', () => {
        console.log('File download initiated.');
    });
});
